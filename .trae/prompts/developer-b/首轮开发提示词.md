# 开发者 B 首轮开发提示词

请基于《ta说》现有仓库结构，只完成翻译官与分享卡基础骨架：

目标：

1. 搭建 `TranslatorPage`。
2. 搭建结构化结果展示区。
3. 搭建 `ShareCardPage` 静态版。
4. 先使用 mock 数据，不接真实 API。

范围：

- `src/pages/TranslatorPage`
- `src/pages/ShareCardPage`
- `src/features/translator`
- `src/features/share-card`

限制：

1. 不要改聊天主流程。
2. 不要改污染和读档逻辑。
3. 不要把 mock 数据直接写死在页面组件内部，尽量抽离 service。

验收：

- 翻译官页面支持空态和结果态
- 分享卡页面能展示完整字段
- 代码结构可继续接入真实会话数据
